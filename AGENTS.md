# GreenPocket — 프로젝트 공통 규칙

2026 KB IT's Your Life 해커톤 프로젝트. 독립 초기 청년의 **관리비·전기·수도·도시가스 고지서**를 진단하고, 서울 에코마일리지 규칙 기반 What-if 목표 → 실천·월 페이스 확인 → 마일리지 적립 → 현금 전환·그린포켓 입금 → 출금으로 연결하는 서비스.

개발 기간이 약 1주일로 짧고, 개발 인원 4명이 각자 AI 에이전트로 병렬 작업한다. **아래 규칙은 서로의 작업을 망가뜨리지 않기 위한 최소 조건이다.**

**하단 탭 5개** — 진단 · 혜택 · What-if · 포켓 · 마이페이지. **What-if 탭이 홈**이며, 온보딩을 마치면 항상 `WF-06`으로 진입한다(결정 C-1).

---

## 1. 작업 전 반드시 읽을 문서

코드를 쓰기 전에 해당 기능의 명세를 먼저 읽는다. 문서 간 내용이 충돌하면 아래 우선순위를 따른다.

| 우선순위 | 문서 | 내용 |
| --- | --- | --- |
| **1 (최상위)** | `docs/feature-spec/기능명세서.md` | 기능 ID, 규칙·데이터·예외, 완료 조건, 우선순위(P0/P1/P2) |
| 2 | `docs/database/schema.sql` | 스키마 기준 원본. 테이블·컬럼·ENUM·제약 |
| 3 | `docs/api/api-spec.md` | 공통 규약, 엔드포인트 60개, 매핑표, 검증 체크리스트 |
| 참고 | `docs/개발기획서.md` | 배경·목표·데이터 출처. 구현 세부의 근거로 삼지 않는다 |

`docs/feature-spec/기능명세서.xlsx`가 기능명세의 **원본**이다. `.md`는 읽기용 사본이라 두 파일이 다르면 엑셀이 기준이며, 「결정 사항」·「확인 필요 사항」 시트는 엑셀에만 있다.

> ⚠️ **`schema.sql`을 DB에 직접 실행하지 않는다.** 맨 앞에 `DROP TABLE` 13개가 있어 기존 데이터가 전부 사라진다.
> 이 파일은 스키마를 읽고 맞추는 **기준 문서**이고, DB에 적용되는 것은 `backend/src/main/resources/db/migration/`의 Flyway 마이그레이션이다.

### 스펙 문서는 필요한 섹션만 읽는다

기능명세서 83KB · API 명세서 100KB · DDL 34KB다. **통째로 읽지 말고 아래 인덱스로 해당 섹션만 연다.** 대신 **읽지 않은 부분을 상상해서 채우지 않는다.** 근거가 필요하면 그 섹션을 열고, 그래도 없으면 질문한다.

| 담당 영역 | `기능명세서.md` 섹션 | `api-spec.md` 절 |
| --- | --- | --- |
| 공통·데모·내비게이션 | `### 0. 공통 기반` | `# 4. 공통·데모 API (COM)` |
| 프로필 | `#### A-1. 주거 프로필` | `# 5. 프로필 API` |
| 고지서·OCR | `#### A-2. 고지서 데이터화` | `# 6. 고지서 API` |
| 진단 | `#### A-3. 비교 진단` | `# 7. 진단 API` |
| 에코 연동·기준 사용량 | `#### B-1. 에코마일리지 연동·기준 사용량` | `# 8. 에코마일리지 연동 API` |
| 목표·실천 미션 | `#### B-2.` · `#### B-3.` | `# 9. 목표·미션 API` |
| 진행 현황·전달 리포트 | `#### B-4. 진행 현황·전달 리포트` | `# 10. 진행 현황·전달 리포트 API` |
| 평가 결과·마일리지 | `#### B-5. 평가 결과·마일리지 적립` | `# 11. 평가 결과·마일리지 API` |
| 혜택 (녹색생활실천) | `### C. 혜택` | `# 12. 혜택 API` |
| 포켓·전환·출금 | `### D. 자립 저축` | `# 13. 포켓 API` |
| 마이페이지·보관함 | `### E. 마이페이지` | `# 14. 마이페이지·보관함 API` |

**영역과 무관하게 매번 볼 것** — 기능명세서의 `## 핵심 비즈니스 규칙` · `## Green What-if 계산식` · `## 팀 결정 사항`, `api-spec.md`의 `# 1. 공통 규칙` · `# 2. 공통 에러 코드` · `# 3. 열거형(ENUM) 사전`. 자주 쓰는 값은 2번 「빠른 참조」에 옮겨 두었으니 그것으로 충분하면 열지 않아도 된다.

### 작업 유형별 필수 참조

| 작업 | 읽을 것 |
| --- | --- |
| 백엔드 도메인 구현 | 기능명세서 해당 섹션 + `schema.sql` + `api-spec.md` 해당 절 + `backend/AGENTS.md` |
| 프론트 화면 구현 | 기능명세서 해당 섹션 + `api-spec.md` 15.3 「화면 → API」 + `frontend/AGENTS.md` |
| DB 스키마 변경 | `schema.sql` + `backend/AGENTS.md`의 Flyway 규칙 |
| 계산 로직 구현 | 기능명세서의 `## 핵심 비즈니스 규칙`과 `## Green What-if 계산식` |
| 통합 테스트·시연 리허설 | `api-spec.md` 부록 A(호출 순서) + 부록 B(검증 체크리스트) |
| 이슈·PR 작성 | `.github/ISSUE_TEMPLATE/` + `.github/pull_request_template.md` (8번 참고) |

---

## 2. 빠른 참조

`api-spec.md`를 열지 않아도 되도록 자주 쓰는 값만 옮겨 둔 것이다. **여기에 없는 세부는 반드시 `api-spec.md` 원문을 확인한다.**

### ID 접두사 — 문서 전체에서 쓰는 약속

| 종류 | 접두사 | 뜻 |
| --- | --- | --- |
| 화면 ID | `ONB` 온보딩 · `AN` 진단 · `BN` 혜택 · `WF` What-if · `PK` 포켓 · `MY` 마이페이지 | 시안 화면 단위. 목록은 기능명세서 `## 화면 목록` |
| 기능 ID | `COM` 공통 · `A` 생활비 진단 · `B` Green What-if · `C` 혜택 · `D` 자립 저축 · `E` 마이페이지 | 기능명세 105건의 단위. 예 `A-2-03` |
| 결정 ID | `A-1~B-4` (1차) · `C-1~C-13` (2026-09-03 확정) | 기능명세서 `## 팀 결정 사항` |

> 기능 ID `A-1`(주거 프로필)과 결정 ID `A-1`(마일리지 전환 UX)은 **서로 다른 체계다.** 문맥으로 구분한다.

### API 공통 규약 (`api-spec.md` 1·2절 요약)

| 항목 | 값 |
| --- | --- |
| Base URL | `/api/v1` |
| 인증 | `X-Demo-Key: <UUID v4>` 헤더. 로그인 없음(결정 A-4) |
| 인증 예외 | `POST /users` · `GET /meta/**` · `POST /demo/reset` |
| 사용자 식별 | 헤더 → `app_user.id`. **요청 본문·경로에 `userId`를 받지 않는다** |
| 멱등 | `Idempotency-Key` 헤더 — 출금 신청, 전환 완료 처리 |
| 로깅 | `demo_key`는 앞 8자만. **계좌번호 원문은 어떤 로그에도 남기지 않는다** |

모든 응답은 아래 래퍼로 감싼다. 각 절의 Response 예시는 **`data` 안에 들어가는 부분만** 적혀 있다.

```json
{ "success": true,  "data": { }, "error": null, "timestamp": "2026-09-03T18:30:00+09:00" }
{ "success": false, "data": null, "error": { "code": "BILL_DUPLICATED", "message": "...", "field": "...", "details": { } }, "timestamp": "..." }
```

- `error.message`는 **화면에 그대로 띄울 한국어 문장**이다. 개발자용 문구를 넣지 않는다.
- 공통 에러 코드: `INVALID_REQUEST` 400 · `UNAUTHENTICATED_DEMO_KEY` 401 · `NOT_FOUND` 404 · `CONFLICT` 409 · `TOO_MANY_REQUESTS` 429 · `INTERNAL_ERROR` 500 · `EXTERNAL_TIMEOUT` 504. **도메인별 코드는 2.2절에 있고, 새로 만들지 않는다.**

### 데이터 타입 (`api-spec.md` 1.4절)

| 종류 | 형식 | 예 |
| --- | --- | --- |
| 금액·마일리지 | **정수** (원 / M). 소수·문자열 금지 | `43200` · `30000` |
| 사용량 | `DECIMAL(12,3)` | `1340.000` |
| 비율·감축률 | `DECIMAL(7,3)` 퍼센트. **음수는 증가** | `10.500` = 10.5% |
| 신뢰도 | `DECIMAL(5,4)` | `0.8231` |
| 월 | `YYYY-MM` 문자열 (DB는 `DATE`로 1일 저장) | `2026-08` |
| 일시 | ISO-8601 + KST 오프셋 | `2026-09-03T18:30:00+09:00` |
| 열거형 | 대문자 스네이크. `schema.sql`과 1:1 | `ELECTRICITY` |

---

## 3. 스펙 준수 원칙

- 기능명세서에 없는 기능·필드·상태값을 **만들어내지 않는다.**
- API 필드명은 `api-spec.md`에 정의된 것만 사용한다. **프론트와 백엔드를 다른 사람이 개발 중이라, 임의로 만든 필드는 통합 시점에 반드시 깨진다.**
- ENUM 값은 `schema.sql`의 정의와 **1:1**이다(`api-spec.md` 3절). 값을 늘리려면 DDL부터 고친다.
- 명세가 모호하면 코드로 결정하지 말고 **먼저 질문한다.**
- 기능명세서의 기능 ID(`A-2-03`, `B-4-08` 등)를 커밋 메시지나 PR 설명에 인용하면 추적이 쉬워진다.
- **P2 기능은 요청받지 않는 한 구현하지 않는다.** MVP 명시적 제외 범위도 마찬가지다.

### 제외 확정 범위 — 이건 만들지 않는다

기획서 「MVP 범위」의 제외 항목이다. 이전 버전(ver1)에 있던 기능이라 옛 자료에서 발견하더라도 구현 대상이 아니다.

- 청년 주거·생활 **지원제도 매칭·신청 관리**
- **공식 요금표 기반 전기요금 계산 엔진** — 절감액은 `B_base × r ÷ 100` 비례 계산이다
- **절감률 직접 입력형 What-if** — 목표는 5·10·15% 구간 선택이다
- 지역난방(결정 C-11), 개별 수도·도시가스 OCR, 외부 API 실연동, 오픈뱅킹·실계좌 연동
- 마지막 방문 탭 복원(C-1), 포켓 이름 변경(C-2), 고지서 수정 이력(C-3)

---

## 4. 핵심 비즈니스 규칙 (모든 코드에 적용)

이 서비스의 신뢰성을 결정하는 규칙이다. 편의를 위해 우회하지 않는다. 원문은 `기능명세서.md` 「핵심 비즈니스 규칙」 11개 항.

1. **원화 우선.** 감축률과 함께 "줄어드는 요금 −44,090원", "지역 평균보다 +4,300원"처럼 원화 영향을 먼저 보여준다.
2. **돈의 3단계 분리.** ① 예상 마일리지(추정) → ② 적립된 마일리지(확정, 아직 현금 아님) → ③ 그린포켓 입금(실제 잔액). 라벨과 색상으로 항상 구분한다.
3. **적립 근거 제한.** 포켓 잔액 = 마일리지 전환 입금 + 녹색생활실천 지급 완료 입금 중 **`COMPLETED` 거래만**. 예상 마일리지·적립 예정 포인트·"덜 낸 요금"(`savedAmount`)은 잔액에 더하지 않는다.
4. **사용자 동의 필수.** 명시적 동의(`agreed: true`) 없이 마일리지 전환과 출금을 실행하지 않는다.
5. **중복 방지.** 평가 회차당 전환 1회, 전환은 1일 1회, 녹색생활실천 월 수령은 월 1건, 출금은 멱등키. DB UNIQUE 제약으로 보장하고, **같은 `Idempotency-Key` 재요청은 409가 아니라 200으로 기존 거래를 돌려준다.**
6. **기준선 이원화.** 진단은 같은 지역 가구 평균(+작년 동월), What-if는 에코마일리지 규칙대로 **직전 2년 같은 기간 평균**을 쓴다. 두 기준을 섞지 않는다.
7. **판정 근거 공개.** 미션 절감률에는 출처 수치·산출 근거·기관을, 마일리지에는 기준 요금·평가 기간 요금·줄인 금액을 함께 내려준다.
8. **불확실한 수치 미표시.** 배출계수·비교 기준이 검증되지 않으면 숫자를 만들지 않는다. 데이터가 없으면 에러가 아니라 `200` + `available: false` + `unavailableReason`으로 응답한다.
9. **사용자 선택을 말없이 바꾸지 않는다.** 실천 재선택은 제안만 하고, 목표 구간 하향은 2회 연속 미달일 때만 제안한다.
10. **시차 규칙.** 월 페이스는 진단 탭 고지서로, 최종 확정은 에코마일리지 누리집 기준으로 판정한다.
11. **처리 중을 완료로 표시하지 않는다.** OCR·연동·전환·출금이 `SUCCEEDED`/`COMPLETED`가 아니면 완료 화면을 띄우지 않는다. 실패 시 **잔액을 바꾸지 않는다.** 계좌번호 원문은 로그에 남기지 않는다.

### 고정 상수 — 임의로 바꾸지 않는다

| 항목 | 값 |
| --- | --- |
| 탄소 환산계수 | 전기 **424 g/kWh** · 수도 **332 g/㎥** · 도시가스 **2,240 g/㎥** |
| 목표 절감률 | 구간 하한 `r ∈ {5, 10, 15}` (%) |
| 지급 구간 | `5 ≤ R < 10` → 10,000M · `10 ≤ R < 15` → 30,000M · `R ≥ 15` → 50,000M · `R < 5` → 0 (1M = 1원, 연 최대 100,000M) |
| 데모 기준값 | 전기 1,340kWh · 도시가스 108㎥ · 수도 66㎥ → 합산 **11.322%** · 최종 **12.499%** · 전기 탄소비중 **82.5%** |

> 화면 시안의 **10.5% · 12% · 83%** 는 계산과 맞지 않는 값이다(결정 C-13). 시안이 아니라 **계산값**을 따른다.

### 서버·클라이언트 책임 분리

- **서버는 숫자와 enum만 내려준다.** 천 단위 구분·`원`·증감 부호·상태 라벨 포맷팅은 전부 FE 몫이다(COM-06·COM-07).
- 예외적으로 DB에 문장으로 저장된 값(출처 문구·산출 근거·미등록 사유)은 서버가 그대로 내려준다.

---

## 5. 디렉토리 구조

```
greenpocket/
├── AGENTS.md              이 파일 — 규칙 원본
│                          (CLAUDE.md·GEMINI.md는 이 파일을 @import 하는 포인터)
├── .env.example           환경변수 템플릿
├── .github/
│   ├── ISSUE_TEMPLATE/    이슈 템플릿 (기능 개발 / 버그 수정)
│   └── pull_request_template.md
├── backend/               Spring Boot 4.1.1 / Java 21 / MySQL
│   ├── AGENTS.md          백엔드 전용 규칙
│   └── src/main/resources/db/migration/
│                          Flyway 마이그레이션 (적용된 파일은 수정 금지)
├── frontend/              Vue 3 / Vite / JavaScript
│   └── AGENTS.md          프론트엔드 전용 규칙
├── docs/                  스펙 문서 (단일 진실 공급원)
│   ├── README.md          문서 지도 · 우선순위 · 문서 갱신 절차
│   ├── 개발기획서.md       배경·목표·MVP 범위·역할 분담
│   ├── feature-spec/      기능명세서.xlsx (원본) · 기능명세서.md (사본)
│   ├── database/          schema.sql — 스키마 기준 원본 (실제 적용은 Flyway)
│   ├── api/               api-spec.md — 엔드포인트 60개
│   └── design/            design-system.md · tokens.css (작성 예정)
└── infra/
```

> **규칙을 수정할 때는 `AGENTS.md`만 고친다.** `CLAUDE.md`·`GEMINI.md`는 import용 포인터라 여기에 규칙을 직접 적으면 에이전트마다 다른 규칙을 보게 된다.

위 구조는 **목표 형태다.** 아직 만들어지지 않은 것이 있으므로, **있다고 가정하지 말고 작업 전에 직접 확인한다.**

### 기반 작업물 — 없다고 혼자 만들지 않는다

아래는 전원이 공유하는 기반이라 한 사람이 만들면 나머지가 그 형태에 묶인다. **없으면 만들지 말고 사용자에게 알린다.**

| 대상 | 없을 때 |
| --- | --- |
| `backend/.../global/` 공통 응답·예외 클래스 | 각자 만들면 응답 포맷이 갈라져 프론트 연동이 깨진다 |
| `frontend/` Vue 프로젝트 (`package.json`) | 스캐폴딩은 별도 작업이다. 없으면 `frontend/AGENTS.md`의 디렉토리 구조·`npm` 명령어는 아직 유효하지 않다 |
| `docs/design/` 디자인 토큰 (결정 B-3) | 토큰을 임의로 정의하지 않는다 |

### 문서를 고치면 같이 고칠 것

| 무엇을 바꾸나 | 함께 고칠 것 |
| --- | --- |
| 기능 추가·삭제·규칙 변경 | `기능명세서.xlsx` → `기능명세서.md` 사본 → `api-spec.md` 해당 절 → (필요하면) `schema.sql` |
| 컬럼 추가·삭제 | `schema.sql` → 마이그레이션 **새 버전 파일** → `api-spec.md` 응답 필드 |
| API 추가·삭제 | `api-spec.md` 본문 → 15.1 목록 → 15.3 화면 매핑 |
| 팀 결정 | 엑셀 「결정 사항」 시트 → 영향받는 기능 행 → `개발기획서.md` 변경 이력 |

---

## 6. 기술 스택

| 구분 | 기술 |
| --- | --- |
| Backend | Spring Boot 4.1.1, Java 21, Spring Data JPA, Flyway, MySQL |
| Frontend | Vue 3, Vite, JavaScript, Pinia, Vue Router, Tailwind CSS |
| OCR | 외부 OCR API |
| 빌드 | Gradle (backend), npm (frontend) |

**Backend는 TypeScript가 아닌 Java, Frontend는 TypeScript가 아닌 JavaScript다.** 스택을 임의로 바꾸거나 추가하지 않는다.

---

## 7. 작업 범위와 소유권

기능 영역은 `api-spec.md` 15.1 「API 60개 한눈에 보기」의 구분을 그대로 쓴다.

| 영역 | 백엔드 패키지 | API 수 | 대표 화면 |
| --- | --- | --- | --- |
| 공통 (사용자·지역·데모 초기화) | `user/` | 4 | ONB-01 |
| 프로필 | `profile/` | 3 | ONB-02 · MY-02 |
| 고지서·OCR | `bill/` | 9 | AN-02 ~ AN-06 · AN-08 |
| 진단 | `diagnosis/` | 3 | AN-01 · AN-07 |
| 에코마일리지 (연동·목표·미션·진행·결과) | `eco/` | 20 | WF-01 ~ WF-11 |
| 혜택 (녹색생활실천) | `greenlife/` | 5 | BN-01 ~ BN-03 |
| 포켓 | `pocket/` | 14 | PK-01 ~ PK-08 |
| 마이페이지·보관함 | `mypage/` | 2 | MY-01 · MY-03 · MY-04 |

- 위 영역은 담당자가 나뉘어 있다. **요청받은 범위 밖의 도메인 폴더를 수정하지 않는다.**
- `global/` (공통 응답·예외·설정)은 모두가 의존하는 공용 코드다. 수정이 필요하면 **먼저 사용자에게 알리고 승인을 받는다.**
- 다음 파일은 전원이 공유하므로 임의 수정 시 머지 충돌이 발생한다. 변경 전 반드시 알린다.
  - `backend/build.gradle`
  - `backend/src/main/resources/application.yaml`
  - `frontend/package.json`
  - `.env.example`

---

## 8. 이슈 · 브랜치 · 커밋 · PR

작업은 **이슈 등록 → 브랜치 생성 → 커밋 → PR** 순서로 진행한다.

### 8-1. 이슈 — `.github/ISSUE_TEMPLATE/` 템플릿을 사용한다

| 템플릿 | 제목 접두 | 대상 |
| --- | --- | --- |
| `기능-개발.md` | `✨[Feat]` | 기능 추가, 문서, 설정, 리팩터링 |
| `버그-수정.md` | `🐛[Fix]` | 오류, 예외, 잘못된 동작 |

- **템플릿 항목을 지우거나 순서를 바꾸지 않는다.** 해당 없는 항목은 `-`로 채운다.
- `관련 API / 화면 / 테이블`과 `완료 조건`을 비워두지 않는다.
- 기능명세서의 기능 ID(`A-2-03`, `B-1-04` 등)를 상세 내용에 함께 적는다.
- 완료 조건은 기능명세서의 해당 기능 `완료 조건` 열을 근거로 작성한다. 임의로 만들지 않는다.

### 8-2. 브랜치

- `feature/<도메인 또는 화면>` — 예: `feature/bill-ocr`, `feature/pocket-ui`
- **`main`에 직접 push하지 않는다.** PR로 병합한다.

### 8-3. 커밋

- 메시지: `<type>: <한국어 요약>`
  - type: `feat` `fix` `refactor` `chore` `docs` `test`
  - 예: `feat: 전기 고지서 직접 입력 API 추가`
- 한 커밋에 여러 도메인의 변경을 섞지 않는다.
- **커밋은 사용자가 명시적으로 요청할 때만 생성한다.** 임의로 커밋하지 않는다.

### 8-4. PR — `.github/pull_request_template.md` 를 따른다

- 웹에서 PR을 열면 템플릿이 자동 적용된다. **`gh pr create` 등으로 본문을 직접 작성할 때도 템플릿과 같은 구조(`관련 Issue` / `작업 내용` / `확인 사항`)를 그대로 채운다.** 본문을 임의 양식으로 쓰면 템플릿이 무시된다.
- `Closes #<이슈번호>`를 반드시 채운다. 이슈 없이 PR을 올리지 않는다.
- **`확인 사항` 체크박스는 실제로 확인한 것만 체크한다.** 빌드·테스트를 돌리지 않고 "정상 동작을 확인했습니다"를 체크하지 않는다.
- 이 저장소는 **squash merge만 허용**한다. PR 제목이 그대로 `main`의 커밋 제목이 되므로, PR 제목도 `<type>: <한국어 요약>` 형식으로 쓴다.

---

## 9. 전역 금지사항

- **`.env` 파일을 생성·수정·커밋하지 않는다.** 환경변수 추가가 필요하면 `.env.example`만 수정하고 사용자에게 알린다.
- 실제 인증키·API 키·비밀번호·DB 접속 정보를 코드나 문서에 쓰지 않는다.
- **의존성을 임의로 추가·삭제·버전 변경하지 않는다.** 필요하면 이유를 설명하고 승인을 받는다.
- **요청 범위를 벗어난 리팩터링, 파일 삭제, 포맷 일괄 변경을 하지 않는다.** 다른 팀원의 작업과 충돌한다.
- 기존 파일을 지우고 새로 만들지 않는다. 필요한 부분만 수정한다.
- 동작하지 않는 코드를 "완료"라고 보고하지 않는다. 빌드·테스트 결과로 확인한다.
