# GreenPocket — 프로젝트 공통 규칙

2026 KB IT's Your Life 해커톤 프로젝트. 독립 초기 청년의 **전기 고지서**를 진단하고, 절감 시뮬레이션 → 지원제도 매칭 → 모의 저축으로 연결하는 서비스.

개발 기간이 약 1주일로 짧고, 팀원 4명이 각자 AI 에이전트로 병렬 작업한다. **아래 규칙은 서로의 작업을 망가뜨리지 않기 위한 최소 조건이다.**

---

## 1. 작업 전 반드시 읽을 문서

코드를 쓰기 전에 해당 기능의 명세를 먼저 읽는다. 문서 간 내용이 충돌하면 아래 우선순위를 따른다.

| 우선순위 | 문서 | 내용 |
| --- | --- | --- |
| **1 (최상위)** | `docs/feature-spec/기능명세서.md` | 기능 ID, 입력·처리 규칙, 완료 조건, 우선순위(P0/P1/P2) |
| 2 | `docs/database/schema.md` | 테이블·컬럼 정의 |
| 3 | `docs/api/api-spec.md` | 엔드포인트, 요청/응답 |
| 참고 | `docs/개발기획서.md` | 배경·목표·데이터 출처. 구현 세부의 근거로 삼지 않는다 |

### 작업 유형별 필수 참조

| 작업 | 읽을 것 |
| --- | --- |
| 백엔드 도메인 구현 | 기능명세서 해당 섹션 + `schema.md` + `api-spec.md` + `backend/AGENTS.md` |
| 프론트 화면 구현 | 기능명세서 해당 섹션 + `api-spec.md` + `frontend/AGENTS.md` |
| DB 스키마 변경 | `schema.md` + `backend/AGENTS.md`의 Flyway 규칙 |
| 계산 로직 구현 | 기능명세서의 `핵심 비즈니스 규칙`과 `Green What-if 계산식` 섹션 |

> `docs/database/schema.md`와 `docs/api/api-spec.md`는 아직 작성 중일 수 있다.
> **파일이 없으면 구조를 추측해서 만들지 말고 사용자에게 확인한다.**

---

## 2. 스펙 준수 원칙

- 기능명세서에 없는 기능·필드·상태값을 **만들어내지 않는다.**
- API 필드명은 `api-spec.md`에 정의된 것만 사용한다. **프론트와 백엔드를 다른 사람이 개발 중이라, 임의로 만든 필드는 통합 시점에 반드시 깨진다.**
- 명세가 모호하면 코드로 결정하지 말고 **먼저 질문한다.**
- 기능명세서의 기능 ID(`A-2-03`, `B-1-04` 등)를 커밋 메시지나 PR 설명에 인용하면 추적이 쉬워진다.
- **P2 기능은 요청받지 않는 한 구현하지 않는다.** MVP 명시적 제외 범위도 마찬가지다.

---

## 3. 핵심 비즈니스 규칙 (모든 코드에 적용)

이 서비스의 신뢰성을 결정하는 규칙이다. 편의를 위해 우회하지 않는다.

1. **예상값과 확인값을 분리한다.** What-if 결과와 예상 혜택은 참고값이며, 포켓 잔액이나 적립 후보에 **절대 포함하지 않는다.**
2. **확인된 금액만 적립된다.** 사용자가 확인한 절감 참고액과 수령 완료된 실수령액만 적립 후보가 된다.
3. **중복 적립을 막는다.** 하나의 고지서 또는 제도 수령 건은 한 번만 적립된다. DB 유니크 제약으로 보장한다.
4. **임의 수치를 만들지 않는다.** 비교 기준선이 없으면 "비교 데이터 준비 중"으로 처리한다. 값을 추정하거나 그럴듯한 숫자를 하드코딩하지 않는다.
5. **사용자 동의 없이 이체하지 않는다.**
6. **금액은 원화 우선.** 사용량(kWh)보다 원화 영향을 먼저 노출한다.

---

## 4. 디렉토리 구조

```
greenpocket/
├── AGENTS.md              이 파일 (CLAUDE.md, GEMINI.md는 심볼릭 링크)
├── .env.example           환경변수 템플릿
├── backend/               Spring Boot 4.1.1 / Java 21 / MySQL
│   └── AGENTS.md          백엔드 전용 규칙
├── frontend/              Vue 3 / Vite / JavaScript
│   └── AGENTS.md          프론트엔드 전용 규칙
├── docs/                  스펙 문서 (단일 진실 공급원)
│   ├── 개발기획서.md
│   ├── feature-spec/      기능명세서
│   ├── database/          ERD·스키마
│   ├── api/               API 명세
│   └── design/            디자인 규칙
└── infra/
```

---

## 5. 기술 스택

| 구분 | 기술 |
| --- | --- |
| Backend | Spring Boot 4.1.1, Java 21, Spring Data JPA, Flyway, MySQL |
| Frontend | Vue 3, Vite, JavaScript, Pinia, Vue Router, Tailwind CSS |
| OCR | 외부 OCR API |
| 빌드 | Gradle (backend), npm (frontend) |

**Backend는 TypeScript가 아닌 Java, Frontend는 TypeScript가 아닌 JavaScript다.** 스택을 임의로 바꾸거나 추가하지 않는다.

---

## 6. 작업 범위와 소유권

- 백엔드 도메인(`profile`, `bill`, `diagnosis`, `simulation`, `benefit`, `pocket`)은 담당자가 나뉘어 있다. **요청받은 범위 밖의 도메인 폴더를 수정하지 않는다.**
- `global/` (공통 응답·예외·설정)은 모두가 의존하는 공용 코드다. 수정이 필요하면 **먼저 사용자에게 알리고 승인을 받는다.**
- 다음 파일은 전원이 공유하므로 임의 수정 시 머지 충돌이 발생한다. 변경 전 반드시 알린다.
  - `backend/build.gradle`
  - `backend/src/main/resources/application.yaml`
  - `frontend/package.json`
  - `.env.example`

---

## 7. 커밋·브랜치

- 브랜치: `feature/<도메인 또는 화면>` — 예: `feature/bill-ocr`, `feature/pocket-ui`
- **`main`에 직접 push하지 않는다.** PR로 병합한다.
- 커밋 메시지: `<type>: <한국어 요약>`
  - type: `feat` `fix` `refactor` `chore` `docs` `test`
  - 예: `feat: 전기 고지서 직접 입력 API 추가`
- **커밋은 사용자가 명시적으로 요청할 때만 생성한다.** 임의로 커밋하지 않는다.
- 한 커밋에 여러 도메인의 변경을 섞지 않는다.

---

## 8. 전역 금지사항

- **`.env` 파일을 생성·수정·커밋하지 않는다.** 환경변수 추가가 필요하면 `.env.example`만 수정하고 사용자에게 알린다.
- 실제 인증키·API 키·비밀번호·DB 접속 정보를 코드나 문서에 쓰지 않는다.
- **의존성을 임의로 추가·삭제·버전 변경하지 않는다.** 필요하면 이유를 설명하고 승인을 받는다.
- **요청 범위를 벗어난 리팩터링, 파일 삭제, 포맷 일괄 변경을 하지 않는다.** 다른 팀원의 작업과 충돌한다.
- 기존 파일을 지우고 새로 만들지 않는다. 필요한 부분만 수정한다.
- 동작하지 않는 코드를 "완료"라고 보고하지 않는다. 빌드·테스트 결과로 확인한다.
