/*
 * 온보딩 화면 (ONB) — api-spec.md 15.3
 *   ONB-01 시작·이름 등록
 *   ONB-02 주거 프로필
 *
 * 화면을 붙일 때 아래 형태로 추가한다. 컴포넌트는 lazy import 로 둔다.
 *
 *   {
 *     path: '/onboarding/start',
 *     name: 'onb-01-start',
 *     component: () => import('@/views/onboarding/StartView.vue'),
 *   }
 */
export default []
